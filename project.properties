# This file specifies the target eclipse and pde.test.utils versions used in this project
#

# The target Eclipse version specifies the major and minor version of Eclipse that
# this example is intended to work with
#
target.eclipse.version=3.3

# The plugin version for the pde.test.utils plugin
# (should match the target Eclipse version).
#
pde.test.utils.version=${target.eclipse.version}
